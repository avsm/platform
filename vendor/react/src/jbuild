(jbuild_version 1)
(library
  ((name react)
   (public_name react)
   (modules (
react
))
   (wrapped false)))
(library
  ((name react_top)
   (public_name react.top)
   (libraries (compiler-libs.toplevel))
   (modules (
react_top
))
   (wrapped false)))
